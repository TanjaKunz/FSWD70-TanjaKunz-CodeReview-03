// describe your code
describe("Calculation of insurance", function(){

       // what it should do
       it("should Return the prize of insurance",function(){
               //expect something
               expect(calcInsurance("austria", 100, 50)).toEqual(250);
       });

});


describe("Calculation of insurance", function(){

       // what it should do
       it("should Return the prize of insurance",function(){
               //expect something
               expect(calcInsurance("greece", 100, 47)).toEqual(350);
       });

});


describe("Calculation of insurance", function(){

       // what it should do
       it("should Return the prize of insurance",function(){
               //expect something
               expect(calcInsurance("hungary", 100, 50)).toEqual(340);
       });

});